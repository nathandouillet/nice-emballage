---
import { rel } from "../lib/base";
import { Image } from "astro:assets";
import wordmark from "../assets/logo-wordmark-black-v1.png";
---

<footer id="contact" class="site-footer">
  <div class="footer-inner wrap">
    <div class="footer-top">
      <a
        class="footer-name"
        href={rel("/")}
        aria-label="Nice Emballage — accueil"
        ><Image src={wordmark} alt="Nice Emballage" /></a
      >
      <p class="footer-location">
        Ouagadougou, Burkina Faso<br />Depuis 16 ans
      </p>
      <nav class="footer-links" aria-label="Navigation du pied de page">
        <a href={rel("/produits")}>Emballages</a><a href={rel("/aquapur")}
          >Aquapur</a
        ><a href={rel("/machines")}>Machines</a><a
          href={rel("/contact")}>Nous trouver</a
        >
      </nav>
    </div>
    <div class="footer-bottom">
      <span>© 2026 Nice Emballage</span><span
        >Fabricant d’emballages plastiques</span
      >
    </div>
  </div>
</footer>

<style>
  .site-footer {
    padding: 48px 0 24px;
    color: var(--color-ink);
  }
  .footer-top {
    display: grid;
    grid-template-columns: 1.2fr 0.8fr auto;
    gap: 30px;
    align-items: start;
  }
  .footer-name {
    display: block;
    line-height: 0;
  }
  .footer-name :global(img) {
    display: block;
    width: auto;
    height: 62px;
  }
  .footer-location {
    margin: 0;
    color: #505a55;
    font-size: 13px;
    line-height: 1.35;
  }
  .footer-links {
    display: flex;
    gap: 24px;
    font-size: 13px;
  }
  .footer-links a {
    color: inherit;
    text-decoration: none;
  }
  .footer-links a:hover {
    color: var(--color-teal);
  }
  .footer-bottom {
    display: flex;
    justify-content: space-between;
    gap: 30px;
    margin-top: 56px;
    padding-top: 16px;
    color: #68706c;
    font-size: 11px;
  }
  @media (max-width: 760px) {
    .site-footer {
      padding: 44px 0 22px;
    }
    .footer-inner {
      padding-left: 20px;
      padding-right: 20px;
    }
    .footer-top {
      grid-template-columns: 1fr;
      gap: 26px;
    }
    .footer-bottom {
      display: block;
      margin-top: 42px;
    }
    .footer-bottom span {
      display: block;
      margin-top: 6px;
    }
    .footer-links {
      flex-wrap: wrap;
      gap: 12px 20px;
    }
  }
</style>
