---
import { Image } from "astro:assets";
import Layout from "../layouts/Layout.astro";
import heroProductionLine from "../assets/hero-ligne-production-nice-bottle-v4-commercial.png";
import aquapurBottleSeries from "../assets/aquapur-bouteilles-serie-v1.png";
import aquapurBottleLight from "../assets/aquapur-bouteille-lumiere-v1.png";
import preformsMacro from "../assets/preformes-macro-editorial-v1.png";
import machine from "../assets/machine-atelier-mineral-v1.png";
import { rel } from "../lib/base";
---

<Layout>
  <section class="intro wrap">
    <div class="intro-side">
      <figure class="intro-material">
        <Image
          src={heroProductionLine}
          alt="Bouteilles Nice Emballage sur une ligne de production"
        />
      </figure>
    </div>
  </section>

  <section class="statement wrap">
    <p>
      Des emballages <em>fabriqués ici</em>, pour les produits du quotidien.
    </p>
  </section>

  <section class="collection wrap" id="emballages">
    <div class="collection-head">
      <span>Emballages</span><h2>Des formes pour<br />chaque projet</h2><p>
        Bouteilles, bouchons, préformes et films : des solutions adaptées à vos
        produits.
      </p>
    </div>
    <div class="collection-showcase">
      <figure>
        <Image src={aquapurBottleSeries} alt="Série de bouteilles Aquapur" />
        <figcaption><span>Bouteilles</span><span>Les formats</span></figcaption>
      </figure>
      <figure>
        <Image src={aquapurBottleLight} alt="Bouteille portée par la lumière" />
        <figcaption><span>Préformes</span><span>Le départ</span></figcaption>
      </figure>
      <figure>
        <Image src={preformsMacro} alt="Préformes PET Nice Emballage" />
        <figcaption><span>Préformes</span><span>La matière</span></figcaption>
      </figure>
    </div>
    <div class="collection-foot">
      <p>Films d’emballage, formats et solutions spécifiques sur demande.</p><a
        class="collection-link cta-link"
        href={rel("/produits")}>Découvrir les emballages</a
      >
    </div>
  </section>

  <section class="machines" id="machines">
    <div class="machines-inner wrap">
      <div class="machines-copy">
        <span>Machines</span><h2>Pour aller plus loin</h2><p>
          Des équipements pour fabriquer, conditionner et développer vos
          capacités de production.
        </p><a class="cta-link" href={rel("/machines")}>Voir les machines</a>
      </div><figure>
        <Image src={machine} alt="Machine de fabrication industrielle" />
      </figure>
    </div>
  </section>
</Layout>

<style>
  .intro {
    /* 56vw ≈ hauteur de l'image 16/9 : évite un grand vide sur tablette portrait */
    min-height: min(92vh, 56vw);
    padding-top: 4vh;
    padding-bottom: 0;
  }
  h1,
  h2 {
    margin: 0;
    font-family: var(--font-display);
    font-weight: var(--fw-display);
    letter-spacing: var(--ls-display-xl);
    line-height: var(--lh-display-xl);
    }
  .intro-side {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-self: stretch;
  }
  figure {
    margin: 0;
  }
  .intro-material {
    width: 100%;
    height: auto;
    aspect-ratio: 16 / 9;
    overflow: hidden;
  }
  .intro-material :global(img) {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .collection-head > span,
  .machines-copy > span {
    display: block;
    font-size: var(--kicker-size);
    letter-spacing: var(--kicker-ls);
    text-transform: uppercase;
    font-weight: var(--kicker-weight);
  }
  .statement {
    padding-block: 7vw 5vw;
  }
  .statement p {
    max-width: 18ch;
    margin: 0;
    font-family: var(--font-display);
    font-size: clamp(36px, 5.1vw, 82px);
    font-weight: var(--fw-display);
    letter-spacing: var(--ls-display-md);
    line-height: 0.96;
  }
  .statement em {
    color: var(--color-teal);
    font-style: normal;
  }
  .collection {
    padding-top: 6vw;
    padding-bottom: 6vw;
  }
  .collection-head {
    display: grid;
    grid-template-columns: 0.5fr 1fr 0.5fr;
    gap: 25px;
    padding-bottom: 20px;
  }
  .collection-head > span {
    margin: 8px 0 0;
  }
  .collection-head h2 {
    font-size: var(--text-h2);
    }
  .collection-head p {
    align-self: end;
    max-width: 22ch;
    margin: 0;
    font-size: var(--text-body);
    line-height: 1.4;
    color: var(--color-text-soft);
  }
  .collection-showcase {
    display: grid;
    grid-template-columns: 0.92fr 1.2fr 0.82fr;
    gap: 24px;
    align-items: end;
    padding-top: 55px;
  }
  .collection-showcase figure {
    overflow: hidden;
  }
  .collection-showcase figure:nth-child(1) {
    aspect-ratio: 0.82;
  }
  .collection-showcase figure:nth-child(2) {
    aspect-ratio: 1.32;
    margin-bottom: 8vw;
  }
  .collection-showcase figure:nth-child(3) {
    aspect-ratio: 0.83;
  }
  .collection-showcase :global(img) {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .collection-showcase figcaption {
    display: flex;
    justify-content: space-between;
    gap: 10px;
    padding-top: 12px;
    font-size: 12px;
    line-height: 1.2;
  }
  .collection-showcase figcaption span:last-child {
    color: var(--color-text-muted);
    text-align: right;
  }
  .collection-foot {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 30px;
    margin-top: 44px;
  }
  .collection-foot p {
    margin: 0;
    color: var(--color-text-soft);
    font-size: var(--text-small);
    }
  .collection-link {
    display: inline-flex;
    flex: none;
    }
  .machines {
    padding: 6vw 0;
    color: var(--color-ink);
  }
  .machines-inner {
    display: grid;
    grid-template-columns: 0.62fr 1.38fr;
    gap: 6vw;
    align-items: center;
  }
  .machines-copy {
    display: flex;
    flex-direction: column;
    min-height: min(40vw, 590px);
    padding: 0;
  }
  .machines-copy > span {
    margin-bottom: auto;
  }
  .machines h2 {
    max-width: 9ch;
    font-size: var(--text-h2);
    }
  .machines-copy p {
    max-width: 28ch;
    margin: 30px 0 0;
    color: var(--color-text-soft);
    font-size: var(--text-body);
    line-height: 1.4;
  }
  .machines-copy a {
    display: inline-flex;
    width: max-content;
    margin-top: 28px;
    }
  .machines figure {
    display: grid;
    min-height: min(44vw, 650px);
    overflow: hidden;
    background: #e7e9e7;
  }
  .machines figure :global(img) {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: right center;
  }
  @media (pointer: coarse) {
    .collection-link,
    .machines-copy a {
      align-items: center;
      min-height: 44px;
    }
  }
  @media (max-width: 760px) {
    .intro {
      min-height: auto;
      display: block;
      padding-top: 20px;
      padding-bottom: 28px;
    }
    .intro-side {
      display: block;
    }
    .intro-material {
      height: auto;
      aspect-ratio: 1.25;
    }
    .statement {
      padding-block: 40px 48px;
    }
    .statement p {
      font-size: clamp(32px, 10.2vw, 44px); /* tient dans la gouttière dès 360px */
    }
    .collection { padding-block: 48px; }
    .collection-head {
      grid-template-columns: 1fr;
      gap: 20px;
    }
    .collection-head h2 {
      font-size: clamp(40px, 12.2vw, 76px);
    }
    .collection-showcase {
      display: flex;
      width: calc(100vw - var(--gutter));
      margin-left: 0;
      padding: 28px 0 0;
      overflow-x: auto;
      gap: 14px;
      scroll-snap-type: x mandatory;
      overscroll-behavior-x: contain;
      scrollbar-width: none;
    }
    .collection-showcase::-webkit-scrollbar {
      display: none;
    }
    .collection-showcase figure,
    .collection-showcase figure:nth-child(n) {
      min-width: 76vw;
      margin: 0;
      scroll-snap-align: start;
    }
    .collection-showcase figure:nth-child(2) {
      aspect-ratio: 0.82;
    }
    .collection-foot {
      display: block;
      margin-top: 24px;
    }
    .collection-link {
      margin-top: 6px;
      align-items: center;
      min-height: 44px;
    }
    .machines-copy a {
      align-items: center;
      min-height: 44px;
    }
    .machines {
      padding: 56px 0;
    }
    .machines-inner {
      display: block;
    }
    .machines-copy {
      min-height: auto;
      padding: 0;
    }
    .machines h2 {
      font-size: clamp(56px, 16vw, 82px);
    }
    .machines figure {
      display: block;
      min-height: 0;
      margin-top: 32px;
    }
    .machines figure :global(img) {
      display: block;
      height: auto;
      object-fit: cover;
      object-position: center;
    }
  }
</style>
