---
import Layout from "../layouts/Layout.astro";
import MapEmbed from "../components/MapEmbed.astro";

const email = "emballagenice@gmail.com";
const phone = "+226 64 98 66 66";
const phoneHref = "tel:+22664986666";
const whatsapp = "https://wa.me/22664986666";
const coords = "12.411808,-1.418128";
const directions = `https://www.google.com/maps/dir/?api=1&destination=${coords}`;
---

<Layout
  title="Nous trouver — Nice Emballage"
  description="Contactez Nice Emballage à Ouagadougou par téléphone, WhatsApp ou email."
>
  <main class="contact-page">
    <header class="contact-hero wrap">
      <p class="contact-kicker">Nous trouver</p>
      <h1>Parlons de<br />votre projet</h1>
      <p class="contact-hero__lead">
        Pour un besoin d’emballage, une ligne de production ou une demande
        spécifique, notre équipe est à votre écoute.
      </p>
    </header>

    <section class="contact-main wrap" aria-label="Prendre contact">
      <div class="contact-direct">
        <p class="contact-kicker">Nous contacter</p>
        <a href={phoneHref}>{phone}</a>
        <a href={`mailto:${email}`}>{email}</a>
        <a href={whatsapp} target="_blank" rel="noopener">WhatsApp</a>
      </div>

      <form class="contact-form" data-mailto={email}>
        <div class="contact-form__row">
          <label
            ><span>Nom</span><input
              type="text"
              name="nom"
              autocomplete="name"
              required
            /></label
          >
          <label
            ><span>Entreprise</span><input
              type="text"
              name="entreprise"
              autocomplete="organization"
            /></label
          >
        </div>
        <label
          ><span>Email</span><input
            type="email"
            name="email"
            autocomplete="email"
            required
          /></label
        >
        <label
          ><span>Votre message</span><textarea name="message" rows="4" required
          ></textarea></label
        >
        <button class="cta-link" type="submit">Envoyer le message</button>
      </form>
    </section>

    <section class="location" aria-labelledby="location-title">
      <div class="location__map"><MapEmbed coords={coords} /></div>
      <div class="location__info wrap">
        <div>
          <p class="contact-kicker" id="location-title">Notre atelier</p>
          <p class="location__address">
            Borgho, sur la route de Fada<br />Ouagadougou, Burkina Faso
          </p>
          <p class="location__hours">Ouvert tous les jours · 7h30 – 18h00</p>
          <a class="cta-link" href={directions} target="_blank" rel="noopener"
            >Ouvrir dans Google Maps</a
          >
        </div>
      </div>
    </section>
  </main>
</Layout>

<script>
  document
    .querySelectorAll<HTMLFormElement>("form.contact-form")
    .forEach((form) => {
      form.addEventListener("submit", (event) => {
        event.preventDefault();
        const data = new FormData(form);
        const name = String(data.get("nom") ?? "").trim();
        const company = String(data.get("entreprise") ?? "").trim();
        const from = String(data.get("email") ?? "").trim();
        const message = String(data.get("message") ?? "").trim();
        const subject = `Demande de ${name}${company ? ` — ${company}` : ""}`;
        const body = [
          `Nom : ${name}`,
          company ? `Entreprise : ${company}` : null,
          `Email : ${from}`,
          "",
          message,
        ]
          .filter((line) => line !== null)
          .join("\n");
        window.location.href = `mailto:${form.dataset.mailto ?? ""}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;
      });
    });
</script>

<style>
  .contact-page {
    overflow: hidden;
    background: var(--color-paper);
  }
  .contact-hero {
    display: grid;
    grid-template-columns: 1fr minmax(0, 1.45fr) minmax(220px, 0.56fr);
    gap: 28px;
    align-items: end;
    padding-top: 9vw;
    padding-bottom: 8vw;
  }
  .contact-kicker {
    margin: 0;
    font-size: var(--kicker-size);
    font-weight: var(--kicker-weight);
    letter-spacing: var(--kicker-ls);
    text-transform: uppercase;
  }
  .contact-hero h1 {
    grid-column: 2;
    margin: 0;
    font-family: var(--font-display);
    font-size: var(--text-h1);
    font-weight: var(--fw-display);
    letter-spacing: var(--ls-display-xl);
    line-height: var(--lh-display-xl);
    }
  .contact-hero__lead {
    grid-column: 3;
    max-width: 24ch;
    margin: 0;
    color: var(--color-text-soft);
    font-size: var(--text-lead);
    line-height: 1.45;
  }

  .contact-main {
    display: grid;
    grid-template-columns: minmax(250px, 0.72fr) minmax(0, 1.28fr);
    gap: clamp(42px, 9vw, 150px);
    padding-bottom: 9vw;
  }
  .contact-direct {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 15px;
  }
  .contact-direct .contact-kicker {
    margin-bottom: 18px;
  }
  .contact-direct a {
    font-family: var(--font-display);
    font-size: var(--text-h3);
    font-weight: var(--fw-display);
    letter-spacing: var(--ls-display-md);
    line-height: var(--lh-display-md);
    text-decoration: none;
  }
  .contact-direct a:hover {
    color: var(--color-teal);
  }
  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 26px;
  }
  .contact-form__row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 28px;
  }
  .contact-form label {
    display: flex;
    flex-direction: column;
    gap: 11px;
  }
  .contact-form label span {
    font-size: var(--kicker-size);
    font-weight: var(--kicker-weight);
    letter-spacing: var(--kicker-ls);
    text-transform: uppercase;
  }
  .contact-form input,
  .contact-form textarea {
    width: 100%;
    border: 0;
    border-bottom: 1px solid var(--color-ink);
    border-radius: 0;
    background: transparent;
    padding: 10px 0;
    color: var(--color-ink);
    font: inherit;
    font-size: 1rem;
    outline: none;
  }
  .contact-form textarea {
    resize: vertical;
    min-height: 112px;
  }
  .contact-form input:focus,
  .contact-form textarea:focus {
    border-color: var(--color-teal);
  }
  .contact-form button {
    align-self: flex-start;
    margin-top: 8px;
    }

  .location {
    position: relative;
    min-height: clamp(420px, 54vw, 720px);
  }
  .location__map,
  .location__map :global(.google-map),
  .location__map :global(iframe) {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    min-height: 0;
  }
  .location__info {
    position: relative;
    z-index: 1;
    display: flex;
    align-items: flex-end;
    min-height: clamp(420px, 54vw, 720px);
    padding-bottom: clamp(36px, 5vw, 80px);
    pointer-events: none;
  }
  .location__info > div {
    max-width: 400px;
    padding: clamp(25px, 3vw, 42px);
    background: var(--color-paper);
    pointer-events: auto;
  }
  .location__address {
    margin: 22px 0 0;
    font-family: var(--font-display);
    font-size: clamp(1.6rem, 2.7vw, 2.7rem);
    font-weight: var(--fw-display);
    letter-spacing: var(--ls-display-md);
    line-height: 0.95;
  }
  .location__hours {
    margin: 22px 0 0;
    color: var(--color-text-soft);
    font-size: var(--text-small);
    }
  .location__info a {
    margin-top: 28px;
    }

  /* écrans tactiles (tablettes comprises) : zones cliquables ≥ 44px */
  @media (pointer: coarse) {
    .contact-direct a {
      display: inline-flex;
      align-items: center;
      min-height: 44px;
    }
    .contact-form button {
      min-height: 44px;
      padding: 10px 0;
    }
    .location__info a {
      padding: 10px 0;
    }
  }
  @media (max-width: 800px) {
    .contact-hero {
      grid-template-columns: 1fr;
      gap: 28px;
      padding-top: 20px;
      padding-bottom: 72px;
    }
    .contact-hero h1,
    .contact-hero__lead {
      grid-column: auto;
    }
    .contact-hero__lead {
      max-width: 33ch;
    }
    .contact-main {
      grid-template-columns: 1fr;
      gap: 64px;
      padding-bottom: 80px;
    }
    /* zones tactiles confortables (≥ 44px) */
    .contact-direct {
      gap: 4px;
    }
    .contact-direct .contact-kicker {
      margin-bottom: 14px;
    }
    .contact-direct a {
      display: inline-flex;
      align-items: center;
      min-height: 44px;
    }
    .contact-form button {
      min-height: 44px;
      padding: 10px 0;
    }
    .location__info a {
      margin-top: 12px;
      padding: 10px 0;
    }
  }
  @media (max-width: 380px) {
    .contact-hero h1 { font-size: 3.2rem; } /* « votre projet » tient sur une ligne en 320px */
  }
  @media (max-width: 560px) {
    .contact-form__row {
      grid-template-columns: 1fr;
      gap: 26px;
    }
    .location { display:flex; flex-direction:column; min-height:0; }
    .location__map, .location__map :global(.google-map), .location__map :global(iframe) { position:relative; height:360px; }
    .location__info { position:static; display:block; width:100%; min-height:0; padding-top:0; padding-bottom:0; } /* width:100% : sinon les marges auto de .wrap rétrécissent le bloc dans le flex */
    .location__info > div {
      max-width: none;
      width: 100%;
      padding: 32px 0 56px; /* aligné sur la gouttière du reste de la page */
    }
  }
</style>
